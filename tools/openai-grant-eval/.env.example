# Copy to .env only when intentionally enabling a real API-backed experiment.
# Dry-run and tests do not read this file and do not require network access.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
OPENAI_GRANT_EVAL_REAL_API=false
