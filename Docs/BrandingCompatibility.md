# Branding Compatibility

Public-facing name: `Osvayder UE`.

Current release names:

- plugin folder: `Plugins/OsvayderUE/`
- descriptor file: `OsvayderUE.uplugin`
- module name: `OsvayderUE`

These names are the current public release surface. The old product path is not a supported install path for this release.

Attribution:

The project retains attribution to the original upstream Unreal editor-automation foundation where legally relevant. That attribution is history/provenance only; current install instructions, module names, descriptor names, docs, and workflows use `Osvayder UE` / `OsvayderUE`.
